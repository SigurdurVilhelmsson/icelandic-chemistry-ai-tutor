# ⚠️ CRITICAL SECURITY REQUIREMENT ⚠️
# These API keys must ONLY be stored in this backend .env file
# NEVER put API keys in frontend environment variables (VITE_* variables)
# Frontend VITE_* variables are embedded in client JavaScript and can be extracted
# API keys here are kept secure on the server and never exposed to browsers

# Anthropic API Key (Required)
# Get from: https://console.anthropic.com/
# Used for: Claude Sonnet 4 AI responses
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (Required)
# Get from: https://platform.openai.com/api-keys
# Used for: Text embeddings (text-embedding-3-small)
OPENAI_API_KEY=your_openai_api_key_here

# Chroma DB Path (Optional)
# Default: /app/data/chroma_db
CHROMA_DB_PATH=/app/data/chroma_db

# Log Level (Optional)
# Options: DEBUG, INFO, WARNING, ERROR
# Default: INFO
LOG_LEVEL=INFO

# Allowed CORS Origins (Required for kvenno.app)
# Comma-separated list of allowed origins
# Production should include all kvenno.app paths that use this backend
# Note: All year-level deployments share the same backend instance
ALLOWED_ORIGINS=https://kvenno.app,https://www.kvenno.app,http://localhost:5173
