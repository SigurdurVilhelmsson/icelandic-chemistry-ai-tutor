# Backend API Endpoint (Required)
# Development: http://localhost:8000
# Production: https://kvenno.app
# Note: The base URL of the backend - frontend will append /ask, /health, etc.
VITE_API_ENDPOINT=https://kvenno.app

# Base Path for Multi-Path Deployment (Required for kvenno.app)
# This MUST be set before building for each deployment path
# - For 1st year: /1-ar/ai-tutor/
# - For 2nd year: /2-ar/ai-tutor/
# - For 3rd year: /3-ar/ai-tutor/
# Note: Used by Vite to configure asset paths and routing
VITE_BASE_PATH=/

# Azure AD Configuration (Optional - for authentication)
# Get these from Azure AD app registration
# VITE_AZURE_CLIENT_ID=your-azure-client-id
# VITE_AZURE_TENANT_ID=your-azure-tenant-id

# ⚠️ SECURITY WARNING ⚠️
# NEVER put API keys in VITE_ variables!
# VITE_ variables are embedded in the client-side JavaScript bundle
# Anyone can extract them from the browser
# API keys (Anthropic, OpenAI, etc.) must ONLY be in backend/.env
