# Python Backend URL (Linode Server)
PYTHON_BACKEND_URL=https://your-linode-server.com

# Environment
NODE_ENV=development

# For local development, copy this file to .env.local and update values
# NEVER commit .env.local to version control!
